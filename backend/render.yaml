services:
  - type: web
    name: coffee-dashboard-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python data/generate_data.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
